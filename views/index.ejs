<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Url Shortener</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-5 pt-5 text-center fw-bolder text-primary display-2">TINYURL</h1>
        <form action="/shortUrls" method="POST" class="my-4 form-inline">
            <div class="card text-center shadow p-3 mb-5 bg-body rounded position-absolute top-50 start-50 translate-middle" style="width: 45rem;">
                <div class="card-body">
                    <h5 class="card-title fw-bold opacity-75 display-6 mb-5">Paste the URL to be shortened</h5>
                    <form>
                        <div class="input-group input-group-lg mb-3">
                            <input required type="url" name="fullUrl" placeholder="Enter the link here" class="form-control p-2" id="fullUrl">
                            <button class="btn btn-primary fs-6 fw-bold py-3" type="submit">Shorten URL</button>
                        </div>
                    </form>
                    <p class="card-text mt-4 text-muted">TINYURL is a free tool to shorten a URL or reduce a link <br>Use our URL Shortener to create a shortened link making it easy to remember</p>
                </div>
            </div>
        </form>
    
        <div class="card text-center shadow p-3 my-5 bg-body rounded position-absolute top-100 start-50 translate-middle" style="width: 45rem;">
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr class="fs-5">
                            <th class="col-4">Full Url</th>
                            <th class="col-4">Converted Url</th>
                            <th class="col-4">Visits</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% shortUrls.forEach(shortUrl => { %>
                        <tr>
                            <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                            <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                            <td><%= shortUrl.visits %></td>
                        </tr>
                        <% }) %> 
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</body>
</html>